<h1>New Flat</h1>

<% if @flat.errors.any? %>
  <ul>
    <% @flat.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= render 'form', flat: @flat %>

<%= link_to 'Back', flats_path %>
